import { Meta } from "@storybook/react";
import Home from "./Home";

export default {
  title: "Pages/Home",
  component: Home,
} as Meta;

const Template = () => <Home />;

export const 홈 = Template.bind({});
